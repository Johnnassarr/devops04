# =============================================================================
# Azure DevOps Pipeline - Mottu API
# =============================================================================
# Pipeline de CI - Build, Test e Push para ACR
# NOTA: Deploy stage comentado - requer Azure CLI instalado
# =============================================================================

trigger:
  branches:
    include:
    - main
    - develop
  paths:
    exclude:
    - README.md
    - SETUP-GUIDE.md
    - '*.sh'

pr:
  branches:
    include:
    - main
    - develop

variables:
  # Build variables
  mavenVersion: '3.8.x'
  jdkVersion: '17'
  
  # Azure Service Connection (IMPORTANTE: Configure este valor!)
  azureServiceConnection: '2b4ef3b2-0971-4f1b-89e4-d7bdb1ac7b55'
  
  # Azure variables
  azureRG: 'rg-challenge3-rm556221'
  acrName: 'acrchallenge3rm556221'
  acrLoginServer: 'acrchallenge3rm556221.azurecr.io'
  location: 'eastus'
  
  # Image variables
  imageName: 'mottu-api'
  imageTag: '$(Build.BuildId)'
  
  # ACR Credentials (IMPORTANTE: Configure estas variáveis no Azure DevOps!)
  # acrUsername: '<nome-do-acr>' (geralmente igual ao acrName)
  # acrPassword: '<senha-do-acr>' (configure como SECRET)
  # Para obter: az acr credential show --name acrchallenge3rm556221
  
  # Database variables (Configure no Azure DevOps)
  # dbHost: '<IP-do-banco-obtido-no-setup>'
  # dbPort: '5432'
  # dbName: 'mottu-api'
  # dbUser: 'postgres'
  # dbPassword: '<configured-as-secret>'

stages:
# =============================================================================
# STAGE 1: BUILD & PUSH (CI)
# =============================================================================
- stage: Build
  displayName: 'Build and Push to ACR'
  jobs:
  - job: BuildJob
    displayName: 'Build Application'
    pool:
      name: self-hosted-pool
    
    steps:
    # Checkout code
    - checkout: self
      displayName: '📥 Checkout Repository'
      clean: true
    
    # Setup Java (usar Java instalado no agente)
    - task: PowerShell@2
      displayName: '☕ Verificar Java'
      inputs:
        targetType: 'inline'
        pwsh: false
        script: |
          Write-Host "Verificando Java..."
          java -version
          Write-Host "JAVA_HOME: $env:JAVA_HOME"
    
    # Build with Maven Wrapper (não precisa instalar Maven!)
    - task: PowerShell@2
      displayName: '🔨 Maven: Clean and Build'
      inputs:
        targetType: 'inline'
        pwsh: false
        script: |
          Write-Host "Building with Maven Wrapper..."
          .\mvnw.cmd clean package -DskipTests=false
          
          Write-Host ""
          Write-Host "✓ Build completed!"
          Write-Host "Artifact location: target\"
    
    # Publish test results
    - task: PublishTestResults@2
      displayName: '📊 Publish Test Results'
      condition: always()
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: '**/surefire-reports/TEST-*.xml'
        mergeTestResults: true
        failTaskOnFailedTests: false
        testRunTitle: 'Unit Tests'
    
    # Publish build artifact
    - task: PublishBuildArtifacts@1
      displayName: '📦 Publish Artifact'
      inputs:
        PathtoPublish: '$(System.DefaultWorkingDirectory)/target'
        ArtifactName: 'target'
        publishLocation: 'Container'
    
    # Docker: Build Image
    - task: Docker@2
      displayName: '🐳 Docker: Build Image'
      inputs:
        command: 'build'
        repository: '$(imageName)'
        dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
        tags: |
          $(imageTag)
          latest
    
    # ACR: Login com credenciais
    - task: PowerShell@2
      displayName: '🔐 ACR: Docker Login'
      inputs:
        targetType: 'inline'
        pwsh: false
        script: |
          Write-Host "Fazendo login no ACR: $(acrLoginServer)"
          # Usando credenciais do ACR (configure acrUsername e acrPassword como variáveis)
          if ($env:ACR_PASSWORD) {
            echo $env:ACR_PASSWORD | docker login $(acrLoginServer) --username $(acrName) --password-stdin
            Write-Host "✓ Login realizado com sucesso!"
          } else {
            Write-Host "⚠ ACR_PASSWORD não configurada. Configure as variáveis:"
            Write-Host "  - acrUsername: $(acrName)"
            Write-Host "  - acrPassword: <obtenha com: az acr credential show --name $(acrName)>"
            Write-Host ""
            Write-Host "Tentando continuar sem login explícito..."
          }
      env:
        ACR_PASSWORD: $(acrPassword)
    
    # Docker: Tag for ACR
    - task: PowerShell@2
      displayName: '🏷️ Docker: Tag Images for ACR'
      inputs:
        targetType: 'inline'
        pwsh: false
        script: |
          Write-Host "Tagging images for ACR..."
          docker tag $(imageName):$(imageTag) $(acrLoginServer)/$(imageName):$(imageTag)
          docker tag $(imageName):latest $(acrLoginServer)/$(imageName):latest
          Write-Host "✓ Images tagged"
    
    # Push to ACR
    - task: PowerShell@2
      displayName: '⬆️ ACR: Push Images'
      inputs:
        targetType: 'inline'
        pwsh: false
        script: |
          Write-Host "Pushing images to ACR..."
          docker push $(acrLoginServer)/$(imageName):$(imageTag)
          docker push $(acrLoginServer)/$(imageName):latest
          Write-Host "✓ Images pushed successfully!"
          Write-Host ""
          Write-Host "=========================================="
          Write-Host "BUILD SUMMARY"
          Write-Host "=========================================="
          Write-Host "Image: $(acrLoginServer)/$(imageName):$(imageTag)"
          Write-Host "Latest: $(acrLoginServer)/$(imageName):latest"
          Write-Host "Build ID: $(Build.BuildId)"
          Write-Host "=========================================="

# =============================================================================
# STAGE 2: DEPLOY (CD) - COMENTADO
# =============================================================================
# NOTA: Para habilitar o Deploy, instale o Azure CLI no self-hosted agent:
#       Windows: https://aka.ms/installazurecliwindows
#
# Após instalar, descomente as linhas abaixo e faça commit novamente
#
# - stage: Deploy
#   displayName: 'Deploy to Azure'
#   dependsOn: Build
#   condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
#   jobs:
#   - deployment: DeployJob
#     displayName: 'Deploy to ACI'
#     pool:
#       name: self-hosted-pool
#     environment: 'production'
#     strategy:
#       runOnce:
#         deploy:
#           steps:
#           # Deploy steps aqui...
